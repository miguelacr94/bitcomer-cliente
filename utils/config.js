


//https://temp.loca.lt
export const apiEndpoint =
  process.env.NODE_ENV == "development"

  ? "https://bitcomer.loca.lt/"
    // ? "https://api.bitcomer.com/"
      // ? "http://192.168.20.28:4000/"
      // ? "https://bitcomer.loca.lt/" 
      : "https://api.bitcomer.com/"

//api

export const dominio = process.env.NODE_ENV == 'development'

  ? 'https://bitcomer.com/'
  : 'https://bitcomer.com/'

